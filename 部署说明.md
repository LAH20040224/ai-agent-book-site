# 《深入理解 AI Agent》学习网站 · 上线部署说明

本文件夹就是一个**完整构建好的静态网站**（《深入理解 AI Agent：设计原理与工程实践》，共 10 章 + 94 个配套实验说明，支持 13 种语言切换、全文搜索、夜间模式）。

另外，根目录下还有一个独立页面 `guide.html`——**《零基础也能学透 AI Agent：全书名词通关指南》**，专为不会编程的读者准备。部署后通过 `你的网址/guide.html` 即可访问，适合直接发给零基础的朋友作为读书前的入门读物。

**不需要懂编程**，按下面任意一种方式操作，几分钟后你的朋友就能通过网址打开它。

---

## 方式一：Netlify Drop（最简单，推荐，约 2 分钟）

1. 用电脑浏览器打开 <https://app.netlify.com/drop>
2. 免费注册 / 登录 Netlify（可用邮箱或 GitHub 账号）
3. 把**本文件夹里的所有内容**（注意：是文件夹*里面*的文件，不是文件夹本身）整体拖进网页的虚线框里
4. 等待上传完成（本站约 315 MB，包含全部语言的配图，请耐心等待几分钟）
5. Netlify 会自动生成一个公开网址，形如 `https://xxxx-yyyy.netlify.app`
6. 把这个网址发给朋友即可打开。之后在 Site settings 里还能改成你想要的名字（如 `agent-book.netlify.app`）

## 方式二：Vercel（同样简单）

1. 打开 <https://vercel.com>，免费注册登录
2. 首页点 **Add New → Project → 选择 "Deploy without Git" / 直接拖拽本文件夹内容**
3. 部署完成后得到 `https://xxx.vercel.app` 网址，发给朋友即可

## 方式三：GitHub Pages（免费、长期稳定，官方原版就用它）

1. 注册 GitHub 账号，新建一个公开仓库（例如 `my-agent-book`）
2. 把本文件夹里的**所有内容**上传到该仓库（网页端 Add file → Upload files，或用 GitHub Desktop）
3. 仓库 → **Settings → Pages → Source 选 `main` 分支根目录**，保存
4. 几分钟后通过 `https://你的用户名.github.io/my-agent-book/` 访问

## 方式四：自己的服务器 / 对象存储

把本文件夹内容原样上传到任意静态网站托管（阿里云 OSS、腾讯云 COS、Nginx 服务器目录等）即可，无需任何运行时环境。

---

## 常见问题

- **朋友打开是 404？** 确认上传时 `index.html` 位于网站根目录（打开网址应直接看到首页，而不是文件列表）。
- **国内访问慢？** GitHub Pages 在国内偶尔不稳定；优先推荐 Netlify / Vercel，或使用国内对象存储 + CDN。
- **想更新内容？** 重新构建后整体替换上传即可。
- **只想保留中文内容、缩小体积？** 可以只上传 `index.html`、`404.html`、`assets/`、`book/`、`chapter1/` ~ `chapter10/`、`search/`、`extras/`、`sitemap.xml*`、`robots.txt`，删除 `book-en/`、`book-ja/` 等其他语言目录（约可省 100 MB）。

## 本地预览（自己电脑上看）

在本文件夹中运行：

```bash
npm run dev
```

然后浏览器打开 <http://localhost:7100/> 即可（无需安装任何依赖，只需要电脑装有 Node.js）。
没有 Node.js 的话，也可以用 Python：`python3 -m http.server 7100`。
